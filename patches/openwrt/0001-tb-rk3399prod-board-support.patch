diff --git a/target/linux/rockchip/armv8/config-6.12 b/target/linux/rockchip/armv8/config-6.12
index 6fb6dae..ae3bad4 100644
--- a/target/linux/rockchip/armv8/config-6.12
+++ b/target/linux/rockchip/armv8/config-6.12
@@ -167,6 +167,7 @@ CONFIG_CPU_FREQ_GOV_POWERSAVE=y
 CONFIG_CPU_FREQ_GOV_SCHEDUTIL=y
 # CONFIG_CPU_FREQ_GOV_USERSPACE is not set
 CONFIG_CPU_FREQ_STAT=y
+CONFIG_CPU_FREQ_THERMAL=y
 CONFIG_CPU_IDLE=y
 CONFIG_CPU_IDLE_GOV_MENU=y
 CONFIG_CPU_IDLE_MULTIPLE_DRIVERS=y
@@ -234,6 +235,10 @@ CONFIG_DNOTIFY=y
 CONFIG_DTC=y
 CONFIG_DT_IDLE_GENPD=y
 CONFIG_DT_IDLE_STATES=y
+CONFIG_DRM=y
+CONFIG_DRM_FBDEV_EMULATION=y
+CONFIG_DRM_FBDEV_OVERALLOC=100
+CONFIG_DRM_ROCKCHIP=y
 CONFIG_DUMMY_CONSOLE=y
 CONFIG_DWMAC_DWC_QOS_ETH=y
 CONFIG_DWMAC_GENERIC=y
@@ -251,6 +256,9 @@ CONFIG_FANOTIFY=y
 CONFIG_FHANDLE=y
 CONFIG_FIXED_PHY=y
 CONFIG_FIX_EARLYCON_MEM=y
+CONFIG_FRAMEBUFFER_CONSOLE=y
+CONFIG_FRAMEBUFFER_CONSOLE_DETECT_PRIMARY=y
+# CONFIG_FRAMEBUFFER_CONSOLE_ROTATION is not set
 CONFIG_FRAME_POINTER=y
 CONFIG_FS_IOMAP=y
 CONFIG_FS_MBCACHE=y
@@ -510,7 +518,7 @@ CONFIG_PHY_ROCKCHIP_DP=y
 CONFIG_PHY_ROCKCHIP_EMMC=y
 # CONFIG_PHY_ROCKCHIP_INNO_CSIDPHY is not set
 # CONFIG_PHY_ROCKCHIP_INNO_DSIDPHY is not set
-# CONFIG_PHY_ROCKCHIP_INNO_HDMI is not set
+CONFIG_PHY_ROCKCHIP_INNO_HDMI=y
 CONFIG_PHY_ROCKCHIP_INNO_USB2=y
 CONFIG_PHY_ROCKCHIP_NANENG_COMBO_PHY=y
 CONFIG_PHY_ROCKCHIP_PCIE=y
@@ -574,17 +582,27 @@ CONFIG_RELOCATABLE=y
 CONFIG_RESET_CONTROLLER=y
 CONFIG_RESET_SCMI=y
 CONFIG_RFS_ACCEL=y
+# CONFIG_ROCKCHIP_ANALOGIX_DP is not set
+# CONFIG_ROCKCHIP_CDN_DP is not set
+CONFIG_ROCKCHIP_DW_HDMI=y
+# CONFIG_ROCKCHIP_DW_MIPI_DSI is not set
 CONFIG_ROCKCHIP_ERRATUM_3568002=y
 CONFIG_ROCKCHIP_ERRATUM_3588001=y
 CONFIG_ROCKCHIP_GRF=y
+# CONFIG_ROCKCHIP_INNO_HDMI is not set
 CONFIG_ROCKCHIP_IODOMAIN=y
 CONFIG_ROCKCHIP_IOMMU=y
+# CONFIG_ROCKCHIP_LVDS is not set
 CONFIG_ROCKCHIP_MBOX=y
 CONFIG_ROCKCHIP_PHY=y
 CONFIG_ROCKCHIP_PM_DOMAINS=y
+# CONFIG_ROCKCHIP_RGB is not set
+# CONFIG_ROCKCHIP_RK3066_HDMI is not set
 # CONFIG_ROCKCHIP_SARADC is not set
 CONFIG_ROCKCHIP_THERMAL=y
 CONFIG_ROCKCHIP_TIMER=y
+CONFIG_ROCKCHIP_VOP=y
+# CONFIG_ROCKCHIP_VOP2 is not set
 CONFIG_RODATA_FULL_DEFAULT_ENABLED=y
 CONFIG_RPS=y
 CONFIG_RSEQ=y
diff --git a/target/linux/rockchip/base-files/etc/inittab b/target/linux/rockchip/base-files/etc/inittab
new file mode 100644
index 0000000..12d4223
--- /dev/null
+++ b/target/linux/rockchip/base-files/etc/inittab
@@ -0,0 +1,4 @@
+::sysinit:/etc/init.d/rcS S boot
+::shutdown:/etc/init.d/rcS K shutdown
+tty1::askfirst:/usr/libexec/login.sh
+::askconsole:/usr/libexec/login.sh
diff --git a/target/linux/rockchip/image/armv8.mk b/target/linux/rockchip/image/armv8.mk
index b9f1fca..cf2065f 100644
--- a/target/linux/rockchip/image/armv8.mk
+++ b/target/linux/rockchip/image/armv8.mk
@@ -436,6 +436,25 @@ define Device/radxa_zero-3w
 endef
 TARGET_DEVICES += radxa_zero-3w
 
+define Device/toybrick_tb-rk3399prod
+  $(Device/rk3399)
+  DEVICE_VENDOR := Toybrick
+  DEVICE_MODEL := TB-RK3399ProD
+  DEVICE_DTS := rk3399pro-toybrick-prod
+  SUPPORTED_DEVICES := rockchip,rk3399pro-toybrick-prod
+  KERNEL := kernel-bin | fit none $$(KDIR)/image-rk3399pro-toybrick-prod.dtb
+  KERNEL_INSTALL := 1
+  IMAGES := rootfs.img
+  IMAGE_SIZE := 128m
+  IMAGE/rootfs.img := append-rootfs | check-size 120m | pad-to 128M
+  DEVICE_PACKAGES := block-mount blkid blockdev e2fsprogs fdisk fstrim \
+	lsblk lscpu mount-utils mmc-utils wdctl ca-bundle curl htop jq lsof \
+	strace ethtool -ip-tiny ip-full iperf3 ss tcpdump-mini kmod-fs-exfat \
+	kmod-fs-vfat kmod-usb-hid kmod-usb-storage kmod-usb-storage-uas \
+	pciutils usbutils
+endef
+TARGET_DEVICES += toybrick_tb-rk3399prod
+
 define Device/sinovoip_bpi-r2-pro
   $(Device/rk3568)
   DEVICE_VENDOR := Sinovoip
diff --git a/target/linux/rockchip/patches-6.12/140-arm64-dts-rockchip-build-toybrick-tb-rk3399prod.patch b/target/linux/rockchip/patches-6.12/140-arm64-dts-rockchip-build-toybrick-tb-rk3399prod.patch
new file mode 100644
index 0000000..69117af
--- /dev/null
+++ b/target/linux/rockchip/patches-6.12/140-arm64-dts-rockchip-build-toybrick-tb-rk3399prod.patch
@@ -0,0 +1,14 @@
+From: OpenWrt TB-RK3399ProD port
+Subject: [PATCH] arm64: dts: rockchip: build Toybrick TB-RK3399ProD DTB
+
+Build the TB-RK3399ProD DTB with its PCIe host enabled.
+
+---
+ arch/arm64/boot/dts/rockchip/Makefile | 1 +
+ 1 file changed, 1 insertion(+)
+
+--- a/arch/arm64/boot/dts/rockchip/Makefile
++++ b/arch/arm64/boot/dts/rockchip/Makefile
+@@ -78,1 +78,2 @@
+ dtb-$(CONFIG_ARCH_ROCKCHIP) += rk3399pro-rock-pi-n10.dtb
++dtb-$(CONFIG_ARCH_ROCKCHIP) += rk3399pro-toybrick-prod.dtb
diff --git a/target/linux/rockchip/patches-6.12/141-dt-bindings-regulator-add-tcs4526.patch b/target/linux/rockchip/patches-6.12/141-dt-bindings-regulator-add-tcs4526.patch
new file mode 100644
index 0000000..c1c6d15
--- /dev/null
+++ b/target/linux/rockchip/patches-6.12/141-dt-bindings-regulator-add-tcs4526.patch
@@ -0,0 +1,21 @@
+From: OpenWrt TB-RK3399ProD port
+Subject: [PATCH] dt-bindings: regulator: fan53555: allow TCS4526
+
+The fan53555 regulator driver already has a tcs,tcs4526 match entry and
+handles its chip ID, but the binding only lists TCS4525.  Allow the compatible
+used by the TB-RK3399ProD GPU regulator.
+
+---
+ Documentation/devicetree/bindings/regulator/fcs,fan53555.yaml | 1 +
+ 1 file changed, 1 insertion(+)
+
+--- a/Documentation/devicetree/bindings/regulator/fcs,fan53555.yaml
++++ b/Documentation/devicetree/bindings/regulator/fcs,fan53555.yaml
+@@ -31,6 +31,7 @@ properties:
+           - silergy,syr827
+           - silergy,syr828
+           - tcs,tcs4525
++          - tcs,tcs4526
+       - items:
+           - const: rockchip,rk8601
+           - const: rockchip,rk8600
diff --git a/target/linux/rockchip/patches-6.12/142-dt-bindings-arm-rockchip-add-tb-rk3399prod.patch b/target/linux/rockchip/patches-6.12/142-dt-bindings-arm-rockchip-add-tb-rk3399prod.patch
new file mode 100644
index 0000000..8f0fe3d
--- /dev/null
+++ b/target/linux/rockchip/patches-6.12/142-dt-bindings-arm-rockchip-add-tb-rk3399prod.patch
@@ -0,0 +1,18 @@
+From: OpenWrt TB-RK3399ProD port
+Subject: [PATCH] dt-bindings: arm: rockchip: add Toybrick TB-RK3399ProD
+
+Document the compatible used by the Toybrick RK3399Pro production board.
+
+---
+ Documentation/devicetree/bindings/arm/rockchip.yaml | 5 +++++
+ 1 file changed, 5 insertions(+)
+
+--- a/Documentation/devicetree/bindings/arm/rockchip.yaml
++++ b/Documentation/devicetree/bindings/arm/rockchip.yaml
+@@ -988,1 +988,6 @@
++      - description: Rockchip Toybrick TB-RK3399ProD board
++        items:
++          - const: rockchip,rk3399pro-toybrick-prod
++          - const: rockchip,rk3399pro
++
+       - description: Rockchip Toybrick TB-RK3588X board
diff --git a/target/linux/rockchip/patches-6.12/143-mmc-sdhci-of-arasan-disable-rk3399-cqe.patch b/target/linux/rockchip/patches-6.12/143-mmc-sdhci-of-arasan-disable-rk3399-cqe.patch
new file mode 100644
index 0000000..a1231ad
--- /dev/null
+++ b/target/linux/rockchip/patches-6.12/143-mmc-sdhci-of-arasan-disable-rk3399-cqe.patch
@@ -0,0 +1,30 @@
+From: OpenWrt TB-RK3399ProD port
+Subject: [PATCH] mmc: sdhci-of-arasan: disable CQE on RK3399
+
+Repeated command queue recovery was observed during otherwise successful
+eMMC writes on this RK3399 production platform.  Prefer storage reliability
+for the router workload while retaining HS400 Enhanced Strobe and ADMA.
+
+Use the existing SDHCI broken-CQE quirk with RK3399-specific platform data.
+
+---
+ drivers/mmc/host/sdhci-of-arasan.c | 10 +++++++++-
+ 1 file changed, 9 insertions(+), 1 deletion(-)
+
+--- a/drivers/mmc/host/sdhci-of-arasan.c
++++ b/drivers/mmc/host/sdhci-of-arasan.c
+@@ -1433,5 +1433,13 @@
++static const struct sdhci_pltfm_data sdhci_arasan_rk3399_pdata = {
++	.ops = &sdhci_arasan_cqe_ops,
++	.quirks = SDHCI_QUIRK_CAP_CLOCK_BASE_BROKEN |
++		SDHCI_QUIRK_BROKEN_CQE,
++	.quirks2 = SDHCI_QUIRK2_PRESET_VALUE_BROKEN |
++		SDHCI_QUIRK2_CLOCK_DIV_ZERO_BROKEN,
++};
++
+ static struct sdhci_arasan_of_data sdhci_arasan_rk3399_data = {
+ 	.soc_ctl_map = &rk3399_soc_ctl_map,
+-	.pdata = &sdhci_arasan_cqe_pdata,
++	.pdata = &sdhci_arasan_rk3399_pdata,
+ 	.clk_ops = &arasan_clk_ops,
+ };
diff --git a/target/linux/rockchip/patches-6.12/144-phy-rockchip-typec-orientation-switch.patch b/target/linux/rockchip/patches-6.12/144-phy-rockchip-typec-orientation-switch.patch
new file mode 100644
index 0000000..a5e6724
--- /dev/null
+++ b/target/linux/rockchip/patches-6.12/144-phy-rockchip-typec-orientation-switch.patch
@@ -0,0 +1,276 @@
+From ee5f73f20fb58416c06b576d6fa646fa3cab4c18 Mon Sep 17 00:00:00 2001
+From: TB-RK3399ProD OpenWrt project <noreply@example.invalid>
+Date: Sun, 26 Jul 2026 16:21:16 +0800
+Subject: [PATCH] phy: rockchip: support Type-C host orientation switching
+
+Backport the orientation-switch integration proposed by Rockchip for the
+RK3399 Type-C PHY, limited to the USB host path used by this board.
+
+The board connector is fixed to source/host, so DWC3 and xHCI remain active
+while the connector is unattached.  Record the initial orientation until
+DWC3 selects host mode, then reinitialize the direction-dependent lanes
+before xHCI is registered.  During normal hotplug, preserve the active host
+controller and PHY for same-orientation reconnects; restart only the PHY when
+the physical connector polarity actually changes.
+
+Link: https://lore.kernel.org/all/20260304094152.92-5-kernel@airkyi.com/
+---
+ drivers/phy/rockchip/Kconfig              |   1 +
+ drivers/phy/rockchip/phy-rockchip-typec.c | 178 ++++++++++++++++++++++++++++++
+ 2 files changed, 179 insertions(+)
+
+diff --git a/drivers/phy/rockchip/Kconfig b/drivers/phy/rockchip/Kconfig
+index dcb8e16..3677bfe 100644
+--- a/drivers/phy/rockchip/Kconfig
++++ b/drivers/phy/rockchip/Kconfig
+@@ -107,6 +107,7 @@ config PHY_ROCKCHIP_SNPS_PCIE3
+ config PHY_ROCKCHIP_TYPEC
+ 	tristate "Rockchip TYPEC PHY Driver"
+ 	depends on OF && (ARCH_ROCKCHIP || COMPILE_TEST)
++	depends on TYPEC || TYPEC=n
+ 	select EXTCON
+ 	select GENERIC_PHY
+ 	select RESET_CONTROLLER
+diff --git a/drivers/phy/rockchip/phy-rockchip-typec.c b/drivers/phy/rockchip/phy-rockchip-typec.c
+index 4efcb78..c41ef1f 100644
+--- a/drivers/phy/rockchip/phy-rockchip-typec.c
++++ b/drivers/phy/rockchip/phy-rockchip-typec.c
+@@ -54,6 +54,7 @@
+ 
+ #include <linux/mfd/syscon.h>
+ #include <linux/phy/phy.h>
++#include <linux/usb/typec_mux.h>
+ 
+ #define CMN_SSM_BANDGAP			(0x21 << 2)
+ #define CMN_SSM_BIAS			(0x22 << 2)
+@@ -372,6 +373,7 @@ struct rockchip_typec_phy {
+ 	struct device *dev;
+ 	void __iomem *base;
+ 	struct extcon_dev *extcon;
++	struct typec_switch_dev *sw;
+ 	struct regmap *grf_regs;
+ 	struct clk *clk_core;
+ 	struct clk *clk_ref;
+@@ -383,6 +385,10 @@ struct rockchip_typec_phy {
+ 	struct mutex lock;
+ 
+ 	bool flip;
++	bool host_ready;
++	bool orientation_valid;
++	bool reinit_pending;
++	bool usb3_powered;
+ 	u8 mode;
+ };
+ 
+@@ -898,6 +904,8 @@ static int rockchip_usb3_phy_power_on(struct phy *phy)
+ 		regmap_read(tcphy->grf_regs, reg->offset, &val);
+ 		if (!(val & BIT(reg->enable_bit))) {
+ 			tcphy->mode |= new_mode & (MODE_DFP_USB | MODE_UFP_USB);
++			tcphy->reinit_pending = false;
++			tcphy->usb3_powered = true;
+ 
+ 			/* enable usb3 host */
+ 			tcphy_cfg_usb3_to_usb2_only(tcphy, false);
+@@ -908,6 +916,7 @@ static int rockchip_usb3_phy_power_on(struct phy *phy)
+ 
+ 	if (tcphy->mode == MODE_DISCONNECT)
+ 		tcphy_phy_deinit(tcphy);
++	tcphy->usb3_powered = false;
+ 
+ 	ret = -ETIMEDOUT;
+ 
+@@ -922,6 +931,7 @@ static int rockchip_usb3_phy_power_off(struct phy *phy)
+ 
+ 	mutex_lock(&tcphy->lock);
+ 	tcphy_cfg_usb3_to_usb2_only(tcphy, false);
++	tcphy->usb3_powered = false;
+ 
+ 	if (tcphy->mode == MODE_DISCONNECT)
+ 		goto unlock;
+@@ -935,9 +945,171 @@ unlock:
+ 	return 0;
+ }
+ 
++#if IS_ENABLED(CONFIG_TYPEC)
++/*
++ * Rebuild the lane mapping after a real orientation change.  The caller holds
++ * tcphy->lock.  Keeping the host controller alive across an unattached Type-C
++ * state lets xHCI observe the next connection in the same way as a fixed host
++ * port; only a polarity change requires the PHY itself to be restarted.
++ */
++static int tcphy_reinit_usb3(struct rockchip_typec_phy *tcphy, u8 new_mode,
++			     const char *role)
++{
++	const struct rockchip_usb3phy_port_cfg *cfg = tcphy->port_cfgs;
++	const struct usb3phy_reg *reg = &cfg->pipe_status;
++	int timeout;
++	int ret;
++	u32 val;
++
++	if (tcphy->mode != MODE_DISCONNECT) {
++		tcphy_cfg_usb3_to_usb2_only(tcphy, false);
++		tcphy_phy_deinit(tcphy);
++	}
++	property_enable(tcphy, &cfg->external_psm, true);
++	property_enable(tcphy, &cfg->usb3tousb2_en, false);
++	tcphy->mode = MODE_DISCONNECT;
++
++	ret = tcphy_phy_init(tcphy, new_mode);
++	if (ret)
++		goto err_disconnected;
++
++	for (timeout = 0; timeout < 100; timeout++) {
++		regmap_read(tcphy->grf_regs, reg->offset, &val);
++		if (!(val & BIT(reg->enable_bit))) {
++			tcphy_cfg_usb3_to_usb2_only(tcphy, false);
++			tcphy->mode = new_mode;
++			tcphy->reinit_pending = false;
++			dev_info(tcphy->dev,
++				 "USB3 PHY ready for %s orientation in %s mode\n",
++				 tcphy->flip ? "reverse" : "normal", role);
++			return 0;
++		}
++		usleep_range(10, 20);
++	}
++
++	tcphy_phy_deinit(tcphy);
++	ret = -ETIMEDOUT;
++
++err_disconnected:
++	dev_err(tcphy->dev, "failed to reinitialize USB3 PHY for %s mode: %d\n",
++		role, ret);
++	tcphy->reinit_pending = false;
++	tcphy->mode = MODE_DISCONNECT;
++	return ret;
++}
++
++static int tcphy_set_orientation(struct typec_switch_dev *sw,
++				 enum typec_orientation orientation)
++{
++	struct rockchip_typec_phy *tcphy = typec_switch_get_drvdata(sw);
++	bool flip;
++	int ret = 0;
++
++	mutex_lock(&tcphy->lock);
++	if (orientation == TYPEC_ORIENTATION_NONE) {
++		tcphy->orientation_valid = false;
++		tcphy->reinit_pending = false;
++		goto out_unlock;
++	}
++
++	flip = orientation == TYPEC_ORIENTATION_REVERSE;
++	if (tcphy->flip != flip) {
++		tcphy->flip = flip;
++		tcphy->reinit_pending = true;
++	}
++	tcphy->orientation_valid = true;
++
++	if (tcphy->host_ready && tcphy->usb3_powered &&
++	    tcphy->reinit_pending)
++		ret = tcphy_reinit_usb3(tcphy, MODE_DFP_USB, "host");
++out_unlock:
++	mutex_unlock(&tcphy->lock);
++	return ret;
++}
++
++static void tcphy_unregister_orientation_switch(void *data)
++{
++	struct rockchip_typec_phy *tcphy = data;
++
++	typec_switch_unregister(tcphy->sw);
++}
++
++static int tcphy_register_orientation_switch(struct rockchip_typec_phy *tcphy)
++{
++	struct typec_switch_desc desc = { };
++	struct device_node *np;
++	int ret = 0;
++
++	np = of_get_child_by_name(tcphy->dev->of_node, "usb3-port");
++	if (!np)
++		return 0;
++
++	if (!of_property_read_bool(np, "orientation-switch"))
++		goto out_put_node;
++
++	desc.drvdata = tcphy;
++	desc.fwnode = of_fwnode_handle(np);
++	desc.set = tcphy_set_orientation;
++
++	tcphy->sw = typec_switch_register(tcphy->dev, &desc);
++	if (IS_ERR(tcphy->sw)) {
++		ret = dev_err_probe(tcphy->dev, PTR_ERR(tcphy->sw),
++				    "failed to register Type-C orientation switch\n");
++		goto out_put_node;
++	}
++
++	ret = devm_add_action_or_reset(tcphy->dev,
++				       tcphy_unregister_orientation_switch,
++				       tcphy);
++
++out_put_node:
++	of_node_put(np);
++	return ret;
++}
++#else
++static int tcphy_register_orientation_switch(struct rockchip_typec_phy *tcphy)
++{
++	return 0;
++}
++#endif
++
++static int rockchip_usb3_phy_set_mode(struct phy *phy, enum phy_mode mode,
++				      int submode)
++{
++	struct rockchip_typec_phy *tcphy = phy_get_drvdata(phy);
++	const char *role;
++	u8 new_mode;
++	int ret = 0;
++
++	if (mode == PHY_MODE_USB_HOST) {
++		new_mode = MODE_DFP_USB;
++		role = "host";
++	} else if (mode == PHY_MODE_USB_DEVICE) {
++		new_mode = MODE_UFP_USB;
++		role = "device";
++	} else {
++		return 0;
++	}
++
++	mutex_lock(&tcphy->lock);
++	tcphy->host_ready = mode == PHY_MODE_USB_HOST;
++
++	if (!tcphy->orientation_valid)
++		goto out_unlock;
++
++	if (!tcphy->reinit_pending && tcphy->mode == new_mode)
++		goto out_unlock;
++
++	ret = tcphy_reinit_usb3(tcphy, new_mode, role);
++out_unlock:
++	mutex_unlock(&tcphy->lock);
++	return ret;
++}
++
+ static const struct phy_ops rockchip_usb3_phy_ops = {
+ 	.power_on	= rockchip_usb3_phy_power_on,
+ 	.power_off	= rockchip_usb3_phy_power_off,
++	.set_mode	= rockchip_usb3_phy_set_mode,
+ 	.owner		= THIS_MODULE,
+ };
+ 
+@@ -1183,6 +1355,12 @@ static int rockchip_typec_phy_probe(struct platform_device *pdev)
+ 		phy_set_drvdata(phy, tcphy);
+ 	}
+ 
++	ret = tcphy_register_orientation_switch(tcphy);
++	if (ret) {
++		pm_runtime_disable(dev);
++		return ret;
++	}
++
+ 	phy_provider = devm_of_phy_provider_register(dev, of_phy_simple_xlate);
+ 	if (IS_ERR(phy_provider)) {
+ 		dev_err(dev, "Failed to register phy provider\n");
+-- 
+2.54.0
